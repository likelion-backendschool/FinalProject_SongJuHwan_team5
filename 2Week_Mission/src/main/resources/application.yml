# DATABASE
spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/week2?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: jootang2
    password: jootang2
    driver-class-name: org.mariadb.jdbc.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: jootangbooks@gmail.com
    password: qzjxpzwdyaikndrz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  # JPA
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    logging.level:
      org.hibernate.SQL: debug
